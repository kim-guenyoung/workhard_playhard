/*
//2. 두 개의 실수를 입력 받아 합계와 평균을 구하는 함수를 각각 만들어서 호출하세요.
#include <stdio.h>
float add(float c, float d); //함수 선언
float average(float c, float d);

int main(void)
{
	float a, b;
	printf("실수 2개 입력 : ");
	scanf_s(%f %f", &a, &b);

	printf("합 : %f", add(a, b));
	printf("평균 : %f", average(a, b));
	return 0;
}
float add(float c, float d)
{
	return c + d;
}
float average(float A, float B)
{
	return (c + d) / 2;
}*/