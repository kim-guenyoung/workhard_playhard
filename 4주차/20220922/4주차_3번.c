/*
//문제 3
#include <stdio.h>
void ShowAllData(const int* arr, int len)
{
	int i;
	for (i = 0; i < len; i++)
		printf("%d ", arr[i]);
		arr에 있는 값을 함수 내에서 맘대로 바꾸지 마라.
	//배열 요소 전부 출력해야 하는데 const선언이 없으면 마음대로
	//배열 요소륿 변경할 수도 있음
}*/