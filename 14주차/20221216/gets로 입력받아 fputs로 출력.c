/*
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
    FILE* fp = fopen("ABCD.txt", "wt");
    char str[100];

    gets(str);
    fputs(str, fp);
    fputs("\n", fp);

    gets(str);
    fputs(str, fp);

    fclose(fp);

    return 0;
}*/